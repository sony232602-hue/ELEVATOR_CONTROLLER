# ELEVATOR_CONTROLLER
An Elevator Controller using Verilog HDL is a digital control system designed to manage the movement of an elevator between multiple floors. The controller receives requests from users through floor-selection buttons and determines whether the elevator should move UP, DOWN, or remain IDLE.  The project can be implemented as a FSM
